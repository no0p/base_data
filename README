## rdatapop

This script generates and executes the SQL to create tables in postgres or mysql which mirror available data sets from the data() function in R.

## Usage 

Set the database connection details in the script.

```
Rscript pgdata.r
```
