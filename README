## rdatapop

This script generates and executes the SQL to create tables in postgres which mirror available data sets from the data() function in R.

Set the database connection details in the script and execute.

